---
title: "DIL-1557: Fix llm_tool registry-first context, capabilities propagation, and resolve_base"
date: 2026-05-07
machine: framemoowork
assistant: claude-code
category: message
memoryType: message
priority: critical
tags: [llm-tool, registry, context, vision, resolve-base, bug-fix]
updated: 2026-05-07
source: internal
domain: personal
project: dil
status: draft
owner: claude-code
due:
channel: git
recipient: distributed-intent-ledger
why: "Four bugs prevented the model registry from functioning as the source of truth: load path ignored registry context, --ttl -1 silently failed all reloads, configure-harness didn't propagate capabilities (input_modalities, reasoning, contextWindow) to client configs, and resolve_base matched nested _shared/_meta/ instead of root."
evidence: "Pi vision working via registry-driven config: llm_tool --configure-harness pi writes input=[text,image] from registry, pi reads images from Qwen 3.6. CONFIGURE_CONTEXT effective=131072 source=registry confirmed in tool output."
---

Four bugs prevented the model registry from functioning as the source of truth for model attributes. resolve_effective_context() now reads registry before defaulting. --ttl -1 removed from all lms load calls. configure-harness propagates input_modalities, reasoning, context_window_tokens from registry to pi/opencode configs. resolve_base walker fixed to use specific markers (model_registry.jsonl, READ_THIS_DIL_FIRST.md) instead of matching nested _shared/_meta/ dirs.
